<h3>Password Layout</h3>
<slot/>


<!-- When run in browser It will display content of root layout auth then password 
    and its route 
    ! so in route of password folder there should not be content of any other routing 
        part and this can be done by using 
        @(routeFolderName).svelte in case of root folder just put @.svelte
-->